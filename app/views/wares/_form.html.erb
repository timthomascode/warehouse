<%= form_with(model: ware, local: true) do |form| %>
  <% if ware.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ware.errors.count, "error") %> prohibited this ware from being saved:</h2>

      <ul>
        <% ware.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :price_cents, "Price" %>
    <%= form.text_field :price_cents %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
